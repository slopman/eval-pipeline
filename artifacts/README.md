# Artifacts

- **`manifest-snippets.anonymized.json`** — short, shareable before/after excerpts for `stagingPassportTruth` (varga heuristic fix) plus a note on Composer vs deterministic audits.

Full multi‑MB manifests remain under `astro_bot/staging-runs/<run_id>/manifest.json` and may contain fixture labels and geographic text; do not publish wholesale without redaction.
